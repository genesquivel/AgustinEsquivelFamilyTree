# Netlify configuration for a plain static site (no build step needed).
[build]
  publish = "."

# Send any unknown path back to the single page.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
